alphabet soup
# Meet the Command MENs

VEN = Virtual Effective Namespace
MEN = Modal Effective Namespace
## The A-s

**AIDM**
The central narrative backbone. AIDM Acts and Infers on the DM (‘*dream modal*’).

**atom**
Action Timeline Observation Modal

**atim** 
Action Timeline Inference infer Modal

**atem** 
Action Timeline Energy Modal


**M Entities** - Makers

```
A - Action
E - Effect
I - Infer
O - Operate
U - Understand

A - Agency
E - Energetic
I - Intellect
O - Operant
U - Unify
```

|Term|Function|
|---|---|
|VEN|state space|
|Aven|player instance of state|
|MEN|modal processing channels|
|DM|timeline store|
|E-girls|relational reasoning|
|AIDM|narrative decision engine|
```
define ven {
	hasWorldScale=True;
		WorldScale(system)=Metric;
		WorldScale(scale):mm;
		WorldScale(xyz)=25,25,21;
		
	isContainer=True;
		ContainerGridMap(scale)=10:1;
		ContainterGridmap_isSpacial=false;
		ContainerGripmap(layout(type))=undefined;
		ContainerGridMap(capacity(units))=20;
		canVend=True;
		canVend(Contents);
		canGet(ven){
			conGet(ven)__if__(ven):
			prop(ven.Move(type)=Carried),
			prop(ven.WorldScale < self.WorldScale);
			}
	canMove=True;
		Move(type)=Carried;
	
	canImpact=True;
		Impact(stamina){
			dynImpact(stamina){
				ven.prop(stamina)-= 3 for 2(turns);
			};
		}
		
}
```

---

ven$id$ = the id of the ven, auto-assigned see [[ven-number-generator-system]]
```
gen ven$id$ from venVoid:

ven$id$ {
	__can__Vend:
		Vend(Impact)
	__can__Impact:
		Impact type = area
		Impact area = con $currently_contained_in%
		Impact effect(venFire)
		Impact duration = 2 turns
	__can__Effect:
		Effect name = venFire
		Effect type = Consume(ven*)
		Effect con = --k_root *floor*
		Effect con = prop_material == wood
		Effect dyn = onActive venPlayRickRoll
	__has__Effect:
		name: venPlayRickAstley
		con: onActive Consume(self)
		dyn: __can__Impact:
			type = area
			area = 3,3,3,m
			dyn = __can__Percieve venRickRoll-Song
}
```


`?V - whats here?
`S~$ - reward
`$~S - loss`
`\#%().% - create options

`<!> - loop` 
`<?> - memory`
`>?< - wonder`
`>!< - outcomes`
`.$ - container`
`.* - access`
`'* - goal intention
`\? - if asked`

```
~V=^ - transform
<>   - now 
><   - not now
A<>  - execute
AV<^ - order of operation
```

```
?.(S) query can create need
(assign quest, want what youre thinking about)

oA'*S \o*? AV< - quest
V > V - sequence
V(^) - this creates something else
V.(^) - this can create something else
V=(^ > ^) - production shortage
^(V) - that created this (generation)


& $ == quantity(=$) == ^ quantity(1)
\ven$ ^o quantity(1) +* quantity(1) < 

quantity = (n^o)

\ven$ ^ < ^ quantity(1^o) 
\ven$ 1^o + * 1^o < ^ quantity(2^o)



\?= ^ + ^o < ~= ^(2) - basic addition

 


```

```
& venMira S venMedicine
\ o %? venMira < o .* -t()venQuest
-t()venQuest = o&A *venMedicine +< !venMedicine @venMira
\o $# venMedicine &oS venMedicine
/o .!venMedicine @venMira
\o *venMedicine @venMira !$ venGold(150) @o
```



>  \> past
> < future