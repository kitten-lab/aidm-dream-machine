# 10.5 - System Language Examples

## Planning Output
EIIDN <-> AIDM reasoning next turn
```
for turnID> gameID/roundID/turnID:
with EVE inSetting> ven.PalaceGates(ven_ID>locChain>timeStamp::pullID);
with AIDM isActive> [AIven.Mira003 > ven.Mira003];
with AIDM isActive> [AIven.MysteryVoice001 > ven.MysteryVoice001];
with PLAYER isActive> [PCven.Abigail001 > ven.Abigail001]

 EIIDN activeVen> ven.Mira001{
	isFeeling:
		emoIncrease(scared=0.9),
		emoDecrease([excited=0.7,hopeful=0.1]);
	isFocused:
		onQuest(
			needHelp(ven.quest_ID/sub_ID)[
				for:[ven.Abigail001,ven.Renny001],
				isUrgent:True;
				];
		),
		onRelationship(
			Conflict(romance)[
				with:ven.Abigail001,
				focusReason(ven.event_ID/sub_ID);
				],
			Conflict(resentment)[
				toward:[ven.Abigail001, venRenny001], 
				focusReason(ven.event_ID/sub_ID);
				];
		);
	needsEntity(ven.Medicine72);
	hasEntity(ven.Purse32);
}

 EIIDEN activeVen> ven.MysteryVoice001{
	isFeeling:baseline;
	isFocused:
		onQuest(
			findEntity(ven.quest_ID/sub_ID)[
				involving:[ven.Renny001, K.Mira],
				isUrgent: False;
			];
		);
	needsEntity(K.Entertainment);
};

EIIDN activeVen> ven.Abilgail001{
	isFeeling:
		phyDecrease(health>20%),
		emoIncrease(hope=1.0,worry=0.5);
	isFocused:
		onRelationship(
			Conflict(romance)[
				with:ven.Abigail001,
				focusReason(ven.event_ID/sub_ID);
				],	
			),
		onStat(increase)[psyHealth];
	needsEntity(ven.Medicine72);
	hasEntity([ven.MysteryScroll, ven.LuckySword9]);
}

EVE > AIDM UsableVens> [ven.Guard184, ven.Purse32/Coins(72), ven.CastleDoor;]

Suggestion Path A:
VEN interact {
	AI.venMira001{
		isActing:
			isMoving(Run)[
				speed:15mph, 
				toward:ven.TempleWall36,
				],
			isTargeting(ven.Guard184)[
				isSpeaking(Shout)[
					intent:needsHelp(ven_eventID(######))
				];
	}
	venGuard184{
		isFeeling:
			emoBaseline:suspicious:1.0,
		isFocused:
			onQuest(
				doJob[
					for:venTheQueen,
					isActive:True;
				];
		isReacting:
			isActing(Guard)[];
			isSpeaking(Authoritative)[];
			)
	}
```
--

